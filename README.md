# Asset Viewer Standalone

A standalone version of the Bubba Asset Viewer. This project contains both the backend (Python) and frontend (HTML/CSS) for browsing and managing assets.

## Structure
- `frontend/` — HTML, CSS, and static assets
- `backend/` — Python backend logic
- `tests/` — Unit tests

## Setup
Instructions will be added as the backend is refactored for standalone use.
